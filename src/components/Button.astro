---
export interface Props {
    projectName: string;
}
const { projectName } = Astro.props;
---

<button class="title-button">{projectName}</button>

<style>
    .title-button {
        position: relative;
        width: 30em;
        height: 5em;
        font-size: larger;
        color: #7050ff;
        background-color: transparent;
        border: none;
        text-align: left;
        cursor: pointer;
        display: flex;
        align-items: center;
        gap: 10px;
    }
    .title-button:hover {
        background-color: #7050ff;
        color: #000;
    }
    /* The box to the left of button content*/
    .title-button::before {
        content: "";
        width: 0.8em;
        height: 0.8em;
        border: 2px solid #7050ff;
        background-color: transparent;
        display: inline-block;
        visibility: hidden;
        margin-left: 4px;
    }
    .title-button.selected::before {
        background-color: #7050ff;
        color: #000;
        visibility:visible;
    }
    .title-button.selected:hover::before {
        background-color: #000000;
        border: 2px solid #000000;
    }
</style>